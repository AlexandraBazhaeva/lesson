открывающий (<tag>) и закрывающий (</tag>).
пронумерованный список (ordered list), поэтому тег называется <ol>.
 каждая строчка списка — это элемент списка (list item) — <li>.
А непронумерованный список (unordered list) создаётся с помощью <ul>.
<a href="https://ru.hexlet.io">Хекслет</a>
<a href=" ссылка, и у неё есть атрибут href со значением https://ru.hexlet.io.
Картинка
<img src="https://i.imgur.com/789p0uP.png">